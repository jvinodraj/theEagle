# scripts

Place runnable utility scripts and one-off operational helpers here.

Rules:

1. Keep business logic in `src/`.
2. Keep scripts as thin orchestration wrappers.
3. Prefer names that describe intent, for example `run_easy_score.py`.
