# tests

Use this folder for automated tests.

Suggested layout:

- `tests/unit/` for pure function/module tests
- `tests/integration/` for end-to-end command/report tests

Test files should be named `test_*.py`.
